---
import { Button, Container, TextCardSection } from '@components/odyssey-theme';
import Layout from '../layouts/Page.astro';

const seo = {
	title: 'Niko Börger',
	description: 'Seite nicht gefunden',
};

// SEO title will be updated by language toggle script
if (typeof window !== 'undefined') {
	const savedLang = localStorage.getItem('preferred-language') || 'de';
	if (savedLang === 'en') {
		seo.description = 'Page not found';
	}
}
---

<Layout {seo}>
	<Container>
		<TextCardSection>
			<h1>404</h1>
			<h2 data-i18n-en="Page Not Found 👀">
				Seite nicht gefunden 👀
			</h2>
			<div class="cta-buttons__container">
				<Button href="/">
					<span data-i18n-en="Return Home">
						Zur Startseite
					</span>
				</Button>
			</div>
		</TextCardSection>
	</Container>
</Layout>

<style>
	.cta-buttons__container {
		display: flex;
		flex-wrap: wrap;
	}
	.cta-buttons__container :global(a) {
		margin-right: 1rem;
		margin-bottom: 1rem;
	}
	.code-buttons__container {
		margin: 1rem auto;
		display: flex;
		flex-wrap: wrap;
	}
	.code-buttons__container > a {
		margin-right: 1rem;
		margin-bottom: 1rem;
	}
</style>
